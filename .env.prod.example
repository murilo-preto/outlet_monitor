# Copy this file to .env.prod on the server and fill in real values.
# docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build

# Shared secret required to trigger POST /scrape from the internet.
# Generate one with: openssl rand -hex 24
SCRAPE_SECRET=

# How often (in hours) the API automatically scrapes and stores a new price
# snapshot, on top of any manually triggered scrapes. Defaults to 24 if unset.
HOURS_BETWEEN_FETCH=24

# Token of the Telegram bot used by the `notifier` service.
# Get one from @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=

# Chat that receives an alert when the scraper fails repeatedly. Message
# @userinfobot on Telegram to find your own chat id. Optional — leave empty and
# failures are only recorded in /status and the logs, never pushed.
ADMIN_CHAT_ID=

# How many consecutive failed scrapes trigger that alert. One message is sent
# per outage, not per failure. Defaults to 3 if unset.
SCRAPE_FAILURES_BEFORE_ALERT=3
