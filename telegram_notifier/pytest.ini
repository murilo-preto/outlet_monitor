[pytest]
# `app` is a plain directory next to tests/, not an installed package, so the
# service root has to be importable for `from app.report import ...` to resolve.
pythonpath = .
testpaths = tests
